{
  "name": "mic-babel-preset",
  "version": "1.0.1",
  "description": "The babel preset we use at Mic",
  "main": "dist/index.js",
  "scripts": {
    "test": "miclint",
    "build": "rm -rf dist && mkdir -p dist && babel lib --out-dir dist",
    "watch": "rm -rf dist && mkdir -p dist && babel -w lib --out-dir dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/micnews/mic-babel-preset.git"
  },
  "author": "mic.com",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/micnews/mic-babel-preset/issues"
  },
  "homepage": "https://github.com/micnews/mic-babel-preset#readme",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.0",
    "miclint": "^2.0.0"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-es2015-loose": "^8.0.0",
    "babel-preset-react": "^6.16.0",
    "babel-preset-react-optimize": "^1.0.1",
    "fast-async": "^6.1.2"
  },
  "bundleDependencies": [
    "babel-preset-es2015",
    "babel-preset-es2015-loose",
    "babel-preset-react",
    "babel-preset-react-optimize",
    "fast-async"
  ]
}
